
#Machine Energy Usage Config. This config is synced from server to client.
[usage]
	#Energy per operation tick (Joules).
	enrichmentChamber = "125"
	#Energy per operation tick (Joules).
	osmiumCompressor = "250"
	#Energy per operation tick (Joules).
	combiner = "125"
	#Energy per operation tick (Joules).
	crusher = "125"
	#Energy per operation tick (Joules).
	metallurgicInfuser = "125"
	#Energy per operation tick (Joules).
	purificationChamber = "500"
	#Energy per operation tick (Joules).
	energizedSmelter = "125"
	#Energy per operation tick (Joules).
	digitalMiner = "5000"
	#Energy per operation tick (Joules).
	electricPump = "250"
	#Energy that can be transferred at once per charge operation (Joules).
	chargePad = "2560000"
	#Energy per operation tick (Joules).
	rotaryCondensentrator = "125"
	#Energy per operation tick (Joules).
	oxidationChamber = "500"
	#Energy per operation tick (Joules).
	chemicalInfuser = "500"
	#Energy per operation tick (Joules).
	chemicalInjectionChamber = "1000"
	#Energy per operation tick (Joules).
	precisionSawmill = "125"
	#Energy per operation tick (Joules).
	chemicalDissolutionChamber = "1000"
	#Energy per operation tick (Joules).
	chemicalWasher = "500"
	#Energy per operation tick (Joules).
	chemicalCrystallizer = "1000"
	#Energy per operation tick (Joules).
	seismicVibrator = "125"
	#Energy per operation tick (Joules).
	pressurizedReactionBase = "13"
	#Energy per operation tick (Joules).
	fluidicPlenisher = "250"
	#Energy per operation tick (Joules).
	laser = "25000"
	#Energy per operation tick (Joules).
	formulaicAssemblicator = "250"
	#Energy per operation tick (Joules).
	modificationStation = "250"
	#Energy per operation tick (Joules).
	isotopicCentrifuge = "500"
	#Energy per operation tick (Joules).
	nutritionalLiquifier = "500"
	#Energy per operation tick (Joules).
	antiprotonicNucleosynthesizer = "500000"
	#Energy per operation tick (Joules).
	pigmentExtractor = "500"
	#Energy per operation tick (Joules).
	pigmentMixer = "500"
	#Energy per operation tick (Joules).
	paintingMachine = "250"
	#Energy per chunk per tick (Joules).
	dimensionalStabilizer = "25000"

	#Teleporter
	[usage.teleporter]
		#Base Joules cost for a teleportation.
		teleporterBase = "2500"
		#Joules per unit of distance travelled during teleportation - sqrt(xDiff^2 + yDiff^2 + zDiff^2).
		teleporterDistance = "25"
		#Flat additional cost for interdimensional teleportation. Distance is still taken into account minimizing energy cost based on dimension scales.
		teleporterDimensionPenalty = "25000"

